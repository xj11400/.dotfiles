# dot-zsh

## zshenv

Move the file to "$XDG_CONFIG_HOME"/zsh/.zshenv and export the following environment variable:

```bash
export ZDOTDIR="$HOME/.config/zsh"
```

You can do this in /etc/zshenv (Or /etc/zsh/zshenv, on some distros).
